{
  "name": "web",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "vue-cli-service build",
    "start": "serve -s dist -l $PORT",
    "dev": "vue-cli-service serve"
  },
  "dependencies": {
    "serve": "^14.2.1"
  },
  "devDependencies": {
    "@vue/cli-service": "^5.0.8"
  },
  "engines": {
    "node": "20.x",
    "npm": "10.x"
  }
}
